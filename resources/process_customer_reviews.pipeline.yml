resources:
  pipelines:
    new_pipeline_2026_06_09_13_18:
      name: Process Customer Reviews
      libraries:
        - glob:
            include: ../src/process_customer_reviews/transformations/**
      catalog: ${var.catalog}
      channel: CURRENT
      continuous: false
      photon: true
      root_path: ../src/process_customer_reviews
      schema: ${var.schema}
      serverless: true
