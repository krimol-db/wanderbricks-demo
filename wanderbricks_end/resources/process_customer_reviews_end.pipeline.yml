resources:
  pipelines:
    process_customer_reviews_end:
      name: Process Customer Reviews End
      libraries:
        - glob:
            include: ../src/process_customer_reviews_end/transformations/**
      catalog: ${var.catalog}
      channel: CURRENT
      continuous: false
      photon: true
      root_path: ../src/process_customer_reviews_end
      schema: ${var.schema}
      serverless: true
